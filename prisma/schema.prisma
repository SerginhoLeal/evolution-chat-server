// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       String @id @default(uuid())
  name     String
  nickname String @unique
  password String
  email    String
  number   String @unique
  photo    String

  user   Friend[] @relation("friend")
  target Friend[] @relation("target")

  created_at DateTime @default(now())

  @@map("users")
}

// Friend is a friend list
model Friend {
  id        String  @id @default(cuid())
  user_id   String
  target_id String
  type_chat String?

  user   User @relation("friend", fields: [user_id], references: [id])
  target User @relation("target", fields: [target_id], references: [id])

  @@map("friends")
}

// model Room {
//   id         String   @id @default(cuid())
//   // instance_id String
//   profile_id String // logged user
//   // friend_id  String
//   // contact_id String // client id
//   created_at DateTime @default(now())

//   messages Message[]

//   // profile Profile?  @relation("profile", fields: [profile_id], references: [id])
//   // room    Profile[] @relation("room")
//   // friend Profile @relation("friend", fields: [friend_id], references: [id])

//   @@map("rooms")
// }

model Message {
  id        String @id @default(cuid())
  friend_id String

  name         String
  number       String
  message      String?
  message_type String
  file         String?
  width        String?
  height       String?
  created_at   DateTime @default(now())

  @@map("messages")
}
